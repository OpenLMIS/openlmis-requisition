version: "2"
services:

  requisition:
    image: openlmis/dev:4.1
    ports:
      - "8080:8080"
      - "5005:5005"
    volumes:
      - '.:/app'
      - 'gradlecache:/gradle'
  db:
    ports:
      - "5432"

volumes:
  gradlecache:
    external: false
